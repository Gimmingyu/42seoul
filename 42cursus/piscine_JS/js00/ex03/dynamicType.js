const dynamicType = {
	data = undefined,
	put: function(num) {
	    if (this.data == undefined)
   	    this.num = input;
	}
	change: function(type) {
	    if (this.data != undefined) {
	        it (typeof type == 'string') {
		    if (type == 'String') {
		        if (Array.isArray(this.data) == true)
			    this.data = String(this.data[0]);
		    }
		}
	},
	printType: function() {
		console.log(type of 
	}
};
